<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền -->
    <solid android:color="@color/white" />

    <!-- Bo góc -->
    <corners android:radius="12dp" />

    <!-- Viền -->
    <stroke
        android:width="1dp"
        android:color="#CCCCCC" /> <!-- hoặc dùng @color/your_border_color -->
</shape>
